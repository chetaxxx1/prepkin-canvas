Build the home screen widget and finish notifications for the Prepkin iPhone app, in this repo (/Users/georgeshi/Desktop/app/prepkin-canvas). These are the two ways the app reaches a student who has not opened it, which is most days. Both were planned on 2026-09-09 and neither is built: there is no widget target, and the notification planner still sends "Anything left for today?" instead of the real list. Every screen below copies a named screen from Finch or Apple; open the links and look before you build.

Read first, in this order:
1. The plan, with the drawings: the artifact "Prepkin Fable Review", sections "Notifications plan" and "Home screen widget" (https://claude.ai/code/artifact/92250c1e-5199-4c4c-b294-ebb7eecde50d). It has the five notification kinds with their exact copy, the widget line table, the thirty-line day bank, and the build steps. This prompt is the build order for that plan.
2. ios/Sources/Core/Notifications.swift (the planner: due, nudge, come-back, shift-end; the scheduler), ios/Sources/AppState.swift (setRemindersEnabled, the Day 1 offer), ios/Sources/DayEditorView.swift (the Reminders card), ios/Sources/HomeView.swift (the Day 1 offer cards, the pattern the widget install card copies).
3. ios/Activity/ and ios/Shared/ (the PrepkinActivity extension target, its slim asset catalogue, FocusActivityAttributes) and ios/project.yml. The widget is a second extension in the same shape; the Live Activity already solved the asset catalogue and the memory ceiling.
4. ios/Sources/Core/Store.swift (save path; the widget snapshot is written beside it), GameState.tasks, and PRODUCT.md house rules: no badge number, no streak, no "you missed", no exclamation marks, quiet hours, every line under 40 characters.
5. Memory facts: the lock screen redraws once a minute, so no ticking seconds and no progress counts on it (live-activity-lock-screen-facts); the running shift is in UserDefaults with the ledger key (focus-shift-survives-a-kill); never use a costume still as a placeholder, the bare stage II still is the stand-in (no-costume-placeholders); check `xcrun simctl list devices booted` and make a session-named simulator (simulator-contention); commit your own files only.

What to copy, and from where:
- Finch's home screen widget: the bird, one line, tap opens the app. Search Mobbin "Finch home screen widget small with the bird and a line", platform ios, look at it, cite the id. Apple's own Weather and Reminders widgets are the size grammar: small is one thing, medium is a list.
- Finch's notification pre-prompt: "Get reminders from Lee" with a mock notification card shown before the system prompt. https://mobbin.com/screens/3e88469e-f60a-439d-ac4d-0bd06fd8eae4. Copy the shape exactly: a preview of our own notification above the button.
- Finch's per-goal reminder time picker after a goal is done: https://mobbin.com/screens/13453dd2-3f02-4946-8549-54d8ce5135bd. Ours is one time for the evening check-in, not per task.
- Anti-reference: Duolingo's streak widget and its red-flame nag. Nothing on our widget or in our notifications counts days or warns.

Do these, in this order, one commit each, tests green after each:

1. Notifications, the copy. The evening check-in body is built from the real open tasks: "2 left today" / "Essay outline, then the walk. Then you're done." Two titles, then "then you're done"; skipped when the list is clear. The due reminder title becomes "Due Wed 8:00 AM · BIO 101", body keeps the kin line. Come-back and shift-end stay as built. Drop `.badge` from the permission request. A unit test asserts no planned title or body contains "streak", "missed", "lose", or "!". Core/Notifications.swift.

2. Notifications, the Done action. A notification category with one action, "Done", on every notification that names a task. Tapping it writes a done-mark for that task id to the app group container; the app reads the marks on next open, marks the tasks done, pays the coins, plays the check animation. Coins are never paid from the notification extension. Tapping a shift-end notification opens Focus on the report (the 2026-09-10 open item). New NotificationActions.swift, the app delegate, AppState.

3. Notifications, the preview sheet. The Day 1 card "Want Moss to check in tomorrow evening?" opens a half sheet before the system prompt: our evening check-in drawn as an iOS banner (Finch's shape), a time picker set to 7 PM, "Turn on". "Turn on" triggers the system prompt; a refusal leaves the switch off and the Reminders card shows its "Open Settings" line as today. Focus's first Start asks with its own one-line preview: "So you can put the phone down and still know when it's over." New ReminderPreviewSheet.swift.

4. The app group and the snapshot. `group.com.prepkin.canvas` on the app, PrepkinActivity and the new widget target. `Store.save` also writes `widget.json` to the group container: kin species, stage, costume id, name, coins, today's open task titles and due times, all-done flag, last opened, the shift end time if one is running, and the day-bank seed. Then `WidgetCenter.shared.reloadAllTimelines()`. The widget never reads state.json. Core/Store.swift, Core/WidgetSnapshot.swift, project.yml.

5. The small widget and the lock screen. WidgetKit target PrepkinWidget with a slim WidgetAssets.xcassets holding the sprout stills and one tank plate (copy the Activity target's catalogue rule). Small: the kin still bottom right, the line top left, "Moss · Wed" bottom left. The line follows the table in the plan: something due in 3 hours → "Bio quiz due 8:00 PM."; morning with open work → "3 things today. Bio quiz first."; afternoon, some done → "2 left. Essay outline, then done."; all done → "All done. Moss noticed."; nothing on the list → a line from the thirty-line day bank seeded by the date; not opened in 3+ days → "Moss is still here."; shift running → "On shift". Lock screen: accessoryCircular is the kin face, accessoryRectangular is "2 left today" over the next due title. Timeline entries at now, each due time minus 3 hours, noon, the check-in hour, midnight. Deep links: add the `prepkin://` scheme; small and lock screen open Home. Widget/PrepkinWidget.swift, Widget/DayBank.swift with the thirty lines from the plan.

6. The install card. Same shape as the Day 1 offers on Home, shown once after the second day's first coin: "Want Moss on your home screen?" with "Show me how" and "Not now". "Show me how" opens a three-step sheet with pictures: hold the home screen, tap +, search Prepkin. Finch does exactly this; search Mobbin "Finch add widget to home screen instructions", cite the id. Never repeated. HomeView.swift, new WidgetHowToSheet.swift.

7. The medium widget, last. Four icon spots wide: the line, up to three task rows with boxes, "Tap a box to finish it · 3 left", the coin count, the kin. Boxes are an AppIntent (iOS 17) that writes the same done-mark as the notification action; the app pays on next open. Widget/MarkDoneIntent.swift.

Do not: put a badge number on the icon, a streak or a day count anywhere, a countdown with seconds on the lock screen, a notification outside 8 AM to 10 PM except a due reminder (never after 11 PM), more than one non-due notification a day, or any string over 40 characters. Do not touch the Live Activity, Focus, or any tab's layout. Do not ship a widget still of a costume that has no still; fall back to the plain coat.

Prove it:
- Unit tests: the check-in body from 0, 1, 2 and 5 open tasks; the widget line for each row of the table, at 9 AM, 3 PM and 9 PM; the day bank returns the same line all day and a different one tomorrow; the done-mark round trip pays once and never twice; no planned text contains a banned word.
- On one simulator: add the small widget and the lock screen widgets, screenshot each in the states "3 things today", "2 left", "All done", "nothing on the list", "on shift". Deliver a notification of each kind with `xcrun simctl push` and screenshot the lock screen; tap Done on one and show the task done and the coins paid on next open. Screenshot the preview sheet and the install sheet. Folder design/screenshots/widget-<date>/ with a README naming the Mobbin screen each piece copies.
- Every widget string under 40 characters; the copy sweep test extended to the widget target.

What an A looks like: a student who has not opened the app since Sunday still sees, on Tuesday morning, their own fish on the home screen and one true line about the day. At 7 PM the phone says what is left, in their own task names, and a Done tap finishes one without opening anything. Nothing counts days, nothing warns, nothing has a red number.

Finish with: the screenshots and README, the test count and result, and the commits, one per numbered item. List what is left for a second pass (medium widget polish, notification tap deep links for every kind, the share link).
